function solve(arr, str) {
  let obj = {
    ascOrDesc: (str) => {
      if (str == "asc") {
        arr = arr.sort(a - b);
      } else {
      }
    },
  };
}
solve([14, 7, 17, 6, 8], "asc");
solve([14, 7, 17, 6, 8], "desc");
